version: "3.8"
  
services:
  mongodb: 
    image: mongo
    container_name: mongodb
    ports: [27017:27017]
    volumes: [desafio:/data/db]   
    
volumes: { desafio: } 
  
networks: { net: { driver: bridge } }